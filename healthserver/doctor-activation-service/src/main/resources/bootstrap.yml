spring:
  application:
    name: doctor-activation-service   # Doit correspondre au nom du fichier dans config-repo
  main:
    allow-bean-definition-overriding: true
  cloud:
    config:
      uri: http://localhost:8880
      fail-fast: true
      retry:
        max-attempts: 6