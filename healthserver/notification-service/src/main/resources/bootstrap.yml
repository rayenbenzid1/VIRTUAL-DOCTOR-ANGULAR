spring:
  application:
    name: notification-service
  main:
    allow-bean-definition-overriding: true

  cloud:
    config:
      uri: http://localhost:8880
      fail-fast: true
      retry:
        max-attempts: 6

server:
  port: 8084